# Node.js dependencies
node_modules/
npm-debug.log
yarn-error.log
.pnpm-store/

# Next.js build output
.next/
out/

# Local environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.bak
*.DS_Store
Thumbs.db

# Docker
*.log
*.tmp
*.temp
*.cache
*.pid
*.sock
*.swp

# Docker build context specific ignores (if building from root)
# These are already handled by .dockerignore files in sub-projects,
# but good to have here for completeness if context changes.
# backend/node_modules/
# frontend/node_modules/
# frontend/.next/

# n8n data
n8n-data/
n8n-data.tar.gz

# Sub-project .git directories (if they are submodules, they should be handled differently)
# For now, assuming they are just nested repos that should be ignored.
backend/.git/
frontend/.git/

# Other generated files
*.zip
*.tar
*.tar.gz
*.rar
*.7z
*.log
.modified # from frontend/
